{{ form_start(form) }}

            <div class="row form-group">
                <div class="col-md-6 mb-3 mb-md-0">
                    {{ form_row(form.nom) }}
                </div>
                <div class="col-md-6">
                    {{ form_row(form.prenom) }}
                </div>
            </div>
            <div class="row form-group">
                
                <div class="col-md-12">
                  {{ form_row(form.telephone) }}
                </div>
              </div>
              <div class="row form-group">
                
                <div class="col-md-12">
                  {{ form_row(form.email) }}
                </div>
              </div>
                <div class="row form-group">
                
                <div class="col-md-12">
                  {{ form_row(form.objet) }}
                </div>
              </div>
              <div class="row form-group">
                
                <div class="col-md-12">
                  {{ form_row(form.message) }}
                </div>
              </div>


    <div class="row form-group">
                <div class="col-md-12">
    <button class="btn btn-primary btn-md text-white">{{ button_label|default('Envoyer message') }}</button>
    </div>
    </div>
{{ form_end(form) }}
